{
  "name": "realtime-mock-data-sample",
  "version": "1.0.0",
  "description": "A small sample to show how to generate random mock data and upload it to the Firebase database.",
  "main": "generateMockData.js",
  "scripts": {
    "generate-mock-data": "node buildScripts/generateMockData",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Wilson",
  "license": "MIT",
  "devDependencies": {
    "chalk": "^1.1.3",
    "chance": "^1.0.9",
    "firebase-tools": "^3.9.0"
  }
}
